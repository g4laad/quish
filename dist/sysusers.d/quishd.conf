# System account the quish worker drops to (chroot + setuid target). No home, no
# login shell — the worker only runs quinn/h3/rustls, never a shell.
u quish - "quish privsep worker" /nonexistent /usr/sbin/nologin
